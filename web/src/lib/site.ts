/** Official ArcLaunch identity. Single source of truth for sidebar, footer, docs and the mobile settings card. */
export const X_HANDLE = "ArcLaunch_";
export const X_URL = `https://x.com/${X_HANDLE}`;
// No mailbox is set up on arclaunch.top yet; support goes through X DMs. Add CONTACT_EMAIL here once one exists.
