# /opt/arclaunch/docker-compose.yml — isolated from the existing solanft stack.
# Secrets come from /opt/arclaunch/.env (PRIVATE_KEY, POSTGRES_PASSWORD); never committed.
name: arclaunch

services:
  web:
    build:
      context: ./web
    image: arclaunch/web:latest
    container_name: arclaunch-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:3100:3000"
    environment:
      NODE_ENV: production
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

  db:
    image: postgres:16-alpine
    container_name: arclaunch-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: arclaunch
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-arclaunch}
      POSTGRES_DB: arclaunch
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arclaunch -d arclaunch"]
      interval: 10s
      timeout: 5s
      retries: 10
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

  indexer:
    build:
      context: ./indexer
    image: arclaunch/indexer:latest
    container_name: arclaunch-indexer
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "127.0.0.1:3101:3101"
    environment:
      NODE_ENV: production
      PORT: "3101"
      RPC_URL: ${ARC_RPC_URL:-https://rpc.testnet.arc.io}
      DATABASE_URL: postgres://arclaunch:${POSTGRES_PASSWORD:-arclaunch}@db:5432/arclaunch
      DEPLOYMENTS_FILE: /app/deployments.json
      UPLOAD_DIR: /data/uploads
      PUBLIC_BASE_URL: https://arclaunch.top
      KEEPER_ENABLED: "true"
      KEEPER_PRIVATE_KEY: ${PRIVATE_KEY}
      # distribute when a token has ≥ KEEPER_FEE_THRESHOLD (6dp USDC) accrued, or every KEEPER_MAX_AGE_MS if it traded.
      # Defaults: 0.01 USDC (testnet-friendly; mainnet .env sets 50000000) / 2 hours (9.6 boss decision, was 30 min).
      KEEPER_INTERVAL_MS: "60000"
      KEEPER_FEE_THRESHOLD: ${KEEPER_FEE_THRESHOLD:-10000}
      KEEPER_MAX_AGE_MS: ${KEEPER_MAX_AGE_MS:-7200000}
    volumes:
      - ./contracts/deployments/arc-testnet.json:/app/deployments.json:ro
      - uploads:/data/uploads
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

volumes:
  pgdata:
  uploads:
