# 05 · 服务器与部署架构

> 更新�?026-09-02 · 密码、私钥一律不入库

## 服务器现状（45.205.18.104 · 香港�?
| �?| �?|
|---|---|
| 系统 | Ubuntu 22.04 LTS�? vCPU / 15 GB RAM / 29 GB 磁盘（空�?24 GB�?|
| 已有业务 | Docker 3 容器 `habo-web / habo-api / habo-ws`（Next.js 15 + API + WS，Solana 生态应�?"solanft"�?*不是 Solana 验证节点**，占用极低） |
| 反向代理 | nginx 80/443，Certbot 证书域名 `hzmrbq.com`，`server_name` �?`45.205.18.104 _`（默认站点） |
| 敏感目录 | `/root/eco-keys/`（Solana 程序与管理员 keypair）、`/opt/solanft/keys*`、`/opt/solanft/.env` —�?**我们不碰** |
| 工具 | 宿主机无 node/pnpm/pm2/psql，全部走 Docker；有 git、nginx、ufw |

### 已做的加固（2026-09-02�?
- 开�?`PubkeyAuthentication`，部署密�?`~/.ssh/arclaunch_ed25519`（本机），服务器 `authorized_keys` 已加
- 安装 fail2ban（sshd jail�? 次失败封 24h，递增�?7d；`journalmatch=_SYSTEMD_UNIT=ssh.service`�?- 启用 ufw：仅放行 22 / 80 / 443
- 观察到之前每小时 100+ �?root 密码暴力破解

### 待老板确认后做

- [ ] 老板本人加一�?SSH 公钥 �?**关闭密码登录**（`PasswordAuthentication no`）→ 改掉聊天里发过的 root 密码
- [ ] 22 端口改非标端口或限制来源 IP（可选）

## 域名与备�?
- **不需要备�?*：备案只针对服务器位于中国大陆的网站；本机在香港，`hzmrbq.com` 现有站点即为无备案运行�?- **�?NFT 站共用主�?*：发射台用子�?**`launch.hzmrbq.com`**，nginx 独立 `arclaunch.conf`，不�?`habo.conf`�?- 需要老板�?DNS 加一�?**A 记录：`launch` �?`45.205.18.104`**，然后执�?`node ops/ssh.mjs --script ops/enable-tls.sh` 自动签证书并强制 HTTPS�?- DNS 生效前的预览地址�?*http://45.205.18.104:8080**（nginx :8080 直�?web 容器，证书配好后关掉该端口）�?
## 已部署（2026-09-02�?
| �?| �?|
|---|---|
| 目录 | `/opt/arclaunch/`（`docker-compose.yml`、`web/` 源码、`nginx/arclaunch.conf`�?|
| 容器 | `arclaunch-web`（node:24-alpine，Next standalone，`127.0.0.1:3100 �?3000`，healthcheck�?|
| nginx | `/etc/nginx/conf.d/arclaunch.conf`：`launch.hzmrbq.com` :80 + 预览 :8080 |
| 部署命令 | `node ops/deploy-web.mjs`（打包源�?�?SFTP �?服务�?docker build �?重启 �?自检）；�?`--nginx` 同时�?nginx 配置 |

> 注意：NFT 项目（`E:\soft\huazongnft`）的 `scripts/*-hk.py` �?root **密码**登录部署。关闭密码登录前必须先把它们改为密钥认证�?
## 部署布局（与现有业务隔离�?
```
/opt/arclaunch/
  docker-compose.yml
  .env                      # 只放非密钥配置；keeper 私钥�?docker secret / 环境注入
  web/                      # Next.js 前端（standalone 构建�?  indexer/                  # 链上事件索引 �?Postgres �?REST/WS
  keeper/                   # �?distribute()/execute() 的机器人，钱包只�?gas �?USDC
  nginx/arclaunch.conf      # 挂到 /etc/nginx/conf.d/，独�?server_name
```

| 容器 | 端口（仅 127.0.0.1�?| 说明 |
|---|---|---|
| `arclaunch-web` | 3100 | Next.js |
| `arclaunch-indexer` | 3101 (API) / 3102 (WS) | 监听 Arc RPC/WSS，写 Postgres |
| `arclaunch-db` | 5432（不暴露�?| Postgres 16，volume 持久�?|
| `arclaunch-keeper` | �?| 定时任务，无入站端口 |

nginx 新增 server block（需要一个域名，例如 `arclaunch.xyz` / `app.xxx.com`），Certbot 自动签证书；不改�?`habo.conf`�?
## 安全模型：什么在链上、什么在后台

| �?| 内容 | 被攻破的后果 |
|---|---|---|
| 链上（不可篡改） | Factory / Token / FeeLocker / Treasury / Uniswap V3。资金、分成、LP 锁定、毕业规�?| 唯一能丢钱的地方 �?审计、参�?immutable、不可升�?|
| 链下·只读 | 索引器、前�?| 页面�?打不开，资金不受影响，用户可直接调合约 |
| 链下·辅助 | Keeper、图片存储、评�?| Keeper 钱包只有少量 gas；`distribute()` / `execute()` 任何人可调，创作者可手动 claim |
| 管理后台 | 创建费开关、白名单、CTO 收款地址、前端精�?隐藏 | 链上参数改动需 **多签（Safe�?*；前端标记只影响展示 |

结论�?*后台全挂，用户一分钱不会少；能动钱的只有合约�?*

## 本地运维工具

```bash
node ops/ssh.mjs "uptime"                 # 远程执行单条命令（密钥认证）
node ops/ssh.mjs --script ops/survey.sh   # 上传并执行脚�?```

环境变量：`SRV_HOST` / `SRV_USER` / `SRV_PORT`；仅首次引导可用 `SRV_PW`�?