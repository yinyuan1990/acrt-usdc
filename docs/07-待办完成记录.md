# 07 · 待办完成记录（2026-09-02 晚）

> 对应 `06-测试报告-v1.md` 第 4 节的待办。除 **审计** 与 **多签**（老板要求：测试完全没问题后最后做，避免升级麻烦）外全部完成，且通过浏览器 E2E 复验（16/16 步）。

| 待办 | 状态 | 做法 | 验证 |
|---|---|---|---|
| 平台币 $ArcLaunch | ✅ | 通过我们自己的 Factory 发射 **$ARCL** `0x8c341ccDd9253F736596A5F3B43f20AcB090CCAe`，$10k 起始市值，首购 2 USDC，与用户同规则 | 链上 tx `0x072a333a…79aa` |
| TWAP 回购销毁 | ✅ | `Treasury.configure(ARCL, 1%, ecoFund, 阈值 5 USDC, 单次 2 USDC)`；Keeper 每分钟检查，余额 ≥ 阈值即用 QuoterV2 报价（3% 滑点保护）调 `execute()`；82% 买 ARCL 转 `0x…dEaD`，18% 进生态基金 | 已自动执行 3 次，烧掉 483.8K ARCL；`/api/treasury` 与 `/burn` 页面实时展示每笔 |
| 销毁看板 | ✅ | 索引 `Executed` 事件 → `burns` 表；看板显示国库余额、累计回购、累计销毁、生态基金、下次执行进度、平台币卡片、每笔销毁 tx | 截图 `e2e-05d-burn.png` |
| Thread 讨论区 | ✅ | 链下评论服务：`POST /api/tokens/:a/comments`、`POST /api/comments/:id/like`，**EIP-191 钱包签名**，后端 `verifyMessage` 验签，10 秒限频，时间戳 5 分钟有效；支持回复、点赞、创作者标识 | E2E 发评论 + 点赞成功，`isCreator=true` |
| Logo 上传 | ✅ | `POST /api/upload`（multipart，≤1 MB，魔数校验 png/jpg/gif/webp，sha256 命名）→ `GET /api/uploads/:name`（immutable 缓存）；发币表单支持上传 / URL / emoji 三种 | E2E 上传 1×1 PNG 并回读 `image/png` |
| 钱包支持 | ✅ | EIP-6963 多钱包自动发现（MetaMask / Rabby / OKX…各自成条目）+ Coinbase Wallet SDK；钱包选择弹窗；WalletConnect 已接线，设置 `NEXT_PUBLIC_WC_PROJECT_ID` 即启用（需在 dashboard.reown.com 免费申请） | 单钱包环境直接连接（E2E 路径） |
| WS 实时推送 | ✅ | 前端连 `/ws`，收到 trade / launch / fees / graduated / burn 即失效对应 react-query 缓存，指数退避重连 | 交易后页面秒级刷新 |
| 活动流回购标识 | ✅ | 国库买入（recipient=0xdead）显示为"🔥 Treasury 回购销毁" | — |
| NFT 项目部署脚本改密钥 | ✅ | `E:\soft\huazongnft\scripts\*-hk.py` 17 个脚本：`password=` → `key_filename=~/.ssh/arclaunch_ed25519`，明文密码清除（原来写的是已失效的旧密码）；接力文档已补充说明 | paramiko 密钥登录验证通过 |
| 关闭 root 密码登录 / 轮换密码 | ⏸ 等老板 | 需要老板先提供自己的 SSH 公钥，否则会把老板锁在外面 | — |
| 管理权转 Safe 多签 | ⏸ 按老板要求最后做 | Safe 1.4.1 官方产物已取得（`Safe.json / SafeProxyFactory.json / CompatibilityFallbackHandler.json`），届时自部署到 Arc 并 `transferOwnership` | — |
| 第三方审计 | ⏸ 不在本轮 | — | — |

## 过程中修的 bug

| 问题 | 修复 |
|---|---|
| 索引器 CPU 100% 假死、API 504：`isqrt` 用 `Math.sqrt` 做初值，对 1e50 量级数偏低后逐 1 校正要跑十亿次 | 改为 2 的幂上界 + 单调牛顿迭代，O(log n)，结果与 Solidity `PriceMath` 逐位一致 |
| Keeper `eth_sendTransaction` 被节点拒绝 | 用钱包客户端自带私钥账户本地签名 |
| 回填时分批处理导致 `volume_since_distribute` 被清零 | 全局按 (区块, logIndex) 顺序重放 |

## 现在的测试网参数

| 参数 | 值 |
|---|---|
| 创建费 | 2 USDC（可开关、可白名单减免） |
| 毕业阈值 | 10,000 USDC |
| 发射保护 | 20 区块（≈10s），持仓 ≤5%，单笔 ≤5.5% |
| 分成 | 创作者 75% / 协议 25%（immutable） |
| Keeper 分发 | ≥0.01 USDC 或 10 分钟（主网 50 USDC / 30 分钟） |
| 国库回购 | 阈值 5 USDC，单次 2 USDC，82/18（比例 immutable） |
